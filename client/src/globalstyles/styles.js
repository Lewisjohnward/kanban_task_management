import styled from "styled-components"
import {ImCancelCircle} from "react-icons/im"

export const XIco = styled(ImCancelCircle)`
    position: absolute;
    top: 10px;
    right: 10px;
    color: white;

    &:hover{
        cursor: pointer;
    }
`

